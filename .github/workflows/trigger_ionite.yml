name: Trigger Ionite Update

on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
    branches: [ "main" ]

jobs:
  trigger-ionite-job:
    runs-on: ubuntu-latest
    steps:
      - name: Doing nothing...
        # 'true' is a shell command that does nothing and returns success
        run: true 
